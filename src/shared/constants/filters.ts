// Area Management
export interface AreaFilter {
  search?: string;
  status?: string;
  pagination?: string
}

// Builder Management
export interface BuilderFilter {
  search?: string;
  status?: string;
  pagination?: string
}